import lazyLoad from '../utils/lazyLoad'
import { LayoutIndex } from '@/routers/constant'
import { RouteObject } from '@/routers/interface'
import { lazy } from 'react'

// 首页模块
const <%= name%>: Array<RouteObject> = [
  {
    element: <LayoutIndex />,
    meta: {
      title: '<%= name%>',
    },
    children: [
      {
        path: '<%= path%>',
        element: lazyLoad(lazy(() => import('@/views<%= path%>'))),
        meta: {
          title: '<%= name%>',
          key: 'xx',
        },
      },
    ],
  },
]

export default <%=name%>